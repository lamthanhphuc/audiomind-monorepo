# Placeholder values for loki stack.
# Replace storage and auth settings for production.
loki:
  auth_enabled: false
  commonConfig:
    replication_factor: 1
  storage:
    type: filesystem
  persistence:
    enabled: true
    size: 20Gi

singleBinary:
  replicas: 1

gateway:
  enabled: true

promtail:
  enabled: true
